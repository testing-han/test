name: Test

on:
  workflow_dispatch:

jobs:
  new:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Commit short
        run: |-
          ls -lrth
          BITBUCKET_COMMIT_SHORT=$(echo ${{ github.sha }} | head -c7)
          echo $BITBUCKET_COMMIT_SHORT
          echo ${{ github.workspace }}
          ${{ github.workspace }}/app.sh

  # new1:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Check out test-branches
  #       uses: actions/checkout@v4
  #       with:
  #         repository: testing-han/test-branche
  
