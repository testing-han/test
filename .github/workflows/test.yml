name: test secrets

on:
  push:

env:
 NAME: "${{ secrets.NAME }}"
 COMs: ${{ vars.COMPANY }}
 
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: print name
        run: echo "$NAME"

      - name: print env
        run: |
          printenv

      - name: test 2
        run: echo "${{ github.event.repository.name }}"
